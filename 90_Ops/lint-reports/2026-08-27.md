# Lint Report — 2026-08-27 (Week 2026-W35)

## 0. Tooling note
- `run_lint_scan` returned exit code 9009 (python3 not resolvable in the tool
  environment). The deterministic scanner `90_Ops/scripts/lint_scan.py` exists and
  only checks frontmatter presence under `40_Thesis/`; that tree is empty, so it
  would report 0 fatal issues. All mechanical checks below were therefore performed
  manually via grep/read to compensate.
- `90_Ops/lint-reports/latest.json` is present with
  `severity_count {info:0, warn:0, fatal:0}`.

## 1. Mechanical scan
- **Orphan pages**: 0 — `30_Wiki/` does not exist (no wiki/entity pages at all).
- **Unresolved links**: 7 distinct wikilink targets referenced from `20_Raw/article/`
  with no backing page: `[[SCHEMA]]`, `[[Sources]]`, `[[Evidence]]`,
  `[[Weekly-Reports]]`, `[[Industry-Map]]`, `[[Theses]]`, `[[kg-architecture-method]]`.
  All are vault-internal navigation scaffolds from the KB bootstrap, not industry
  entities.
- **Near-duplicate names**: none (13 distinct raw file names).
- **Stale pages**: none (all raw docs `updated_date: 2026-08-26`).
- **#tbv aging**: none (no `#tbv` markers in vault).
- **assumption-class checks**: ignored (assumption layer retired).
- **Stats**: raw=13, wiki=0, thesis=0, evidence=0, assumption=0, weekly=0.

## 2. Judgment checks
- **Contradiction scan**: N/A — no entity pages / `#fact` statements exist
  (`30_Wiki/` empty). 2026-08-27 is not a scheduled full-scan Sunday; nothing to
  check either way.
- **Atomic thesis integrity**: `40_Thesis/atomic/` directory absent → 0 thesis pages,
  0 missing-citation violations. Nothing to report (directory owned by deterministic
  pipeline; read-only).
- **Rejects report**: `rejects-2026-W35.json` MISSING (also W34 missing). Reject
  count/rate: N/A — no atomic extraction output this week.
- **Build-page candidates**: the only unresolved concepts (≥3 occurrences) are
  vault-internal navigation targets (Sources, SCHEMA, Evidence, etc.) — not industry
  entities → no page built; reason recorded.
- **4-week orphans**: no wiki pages and no prior lint-report history → nothing
  quarantined.

## 3. Dispositions
- No fixes required (no orphans, no violations, no contradictions, no #tbv).
- Flag for pipeline owner: `run_lint_scan` tool is broken (exit 9009) and the
  weekly rejects report is missing for W35.
- Flag for report agent: `15_Weekly/2026-W35.md` base sections were absent at lint
  time (maintenance sections appended by lint agent instead).

## 4. Trend vs last week
- First lint report on record (no prior `90_Ops/lint-reports/*.md`). This week is
  the baseline.
